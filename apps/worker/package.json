{
  "name": "@jamio/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "WRANGLER_WRITE_LOGS=false wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@jamio/game-core": "0.1.0",
    "@jamio/protocol": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260707.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.24.3"
  }
}
